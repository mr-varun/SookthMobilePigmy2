# Project Structure

This file ensures the storage directory structure is maintained in version control.

## Directories

- `backups/` - Database backup files
- `uploads/` - User uploaded files
- `cache/` - Application cache files
- `logs/` - Application log files
